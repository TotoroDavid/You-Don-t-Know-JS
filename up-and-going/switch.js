/*

if (a == 2) {
    // do something
} else if (a == 10) {
    // do another thing 
} else if (a == 42) {
    //do yet another thing 
} else {
    // fallback to here
}

// that's the same than the next example'

switch (a) {
    case 2:
        // do something
        break;
    case 3:
        // do another thing
        break;
    default:
        break;
}

*/

/*

var a = 42

var b = (a > 41) ? 'hello world' : 'hello'

//similar to :

if (a > 41) {
    b = 'hello world'
} else {
    b = 'hello'
}

*/